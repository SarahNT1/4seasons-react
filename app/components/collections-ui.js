import Header from "../components/header";

export default function CollectionsUI(){
    return(
        <div>
            <Header />
            <div>
                This is the collections page.
            </div>
            <div>
                This is where the user will see all the cards they are able to collect and the cards that they have already collected.
            </div>
        </div>
    )
}